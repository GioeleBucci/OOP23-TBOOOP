package tbooop.model.core.api.unmovable;

/** Names for every unmovable object
 *  in the game.
 */
public enum UnmovableName {
    /**Bill pickup name. */
    BILL,
    /**Coin pickup name. */
    COIN,
    /**Heart pickup name. */
    HEART,
    /**Key pickup name. */
    KEY;
}
