package tbooop.model.core.api;

/**
 * A tag for game objects.
 */
public enum GameTag {
    /** Player tag. */
    PLAYER,
    /** Enemy tag. */
    ENEMY,
    /** Boss tag. */
    BOSS,
    /** Pickup tag. */
    PICKUP,
    /** Projectile tag. */
    PROJECTILE,
    /** Door tag. */
    DOOR,
    /** Trapdoor tag. */
    TRAPDOOR;
}
