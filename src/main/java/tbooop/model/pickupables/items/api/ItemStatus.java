package tbooop.model.pickupables.items.api;

/**
 * Rapresents if the item is special
 * or not.
 */
public enum ItemStatus {
    /**Item spawned in the
     * item shop tag.
     */
    SPECIAL,
    /**Normal items tag. */
    NORMAL;
}
